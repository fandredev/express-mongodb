{
    "preset": "ts-jest",
    "transform": {
        "^.+\\.(ts|tsx)?$": ["ts-jest", {
            "tsconfig": "tsconfig.json"
        }]
    },
  "collectCoverage": true,
  "collectCoverageFrom": ["./src/**"]
  
}